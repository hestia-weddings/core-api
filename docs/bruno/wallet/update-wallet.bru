meta {
  name: Update Wallet
  type: http
  seq: 4
}

patch {
  url: {{base-url}}/wallet/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token-couple}}
}

body:json {
  {
    "pixKey": "new-pix-key@example.com"
  }
}
