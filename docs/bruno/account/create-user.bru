meta {
  name: Create User
  type: http
  seq: 2
}

post {
  url: {{base-url}}/account
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token-admin}}
}

body:json {
  {
    "name": "João Silva",
    "email": "joao@example.com",
    "wedding_id": "{{wedding-id}}"
  }
}
